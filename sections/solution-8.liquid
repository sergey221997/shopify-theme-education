{% render 'exercise-8' %}
    {% comment %} {% assign number = "1 | 2 | 5 | 6 | 12 | 1.3 | -1" %}
    {% assign number_arrey = number | split: '|' %}
    {% assign number_sum = number_arrey | sum %}
    {% assign number_mult = number_sum | times: "7" %}
    {% assign number_rounding = number_mult | round %}
    {% assign value = number_rounding %}
    {{ value }} 
{% endcomment %}

{% assign number = "1 | 2 | 5 | 6 | 12 | 1.3 | -1 | 1" %}
{% assign numbers_array = number | split: '|' %}
{% assign count_numbers = numbers_array | size %}

{{ numbers_array | sum | times: count_numbers | round }}

{% render 'exercise-wrapper', Nav: Nav, Content: Content %}

{% schema %}
    {
        "name": "Exercise 8",
        "limit": 1,
        
        "presets": [
            {
                "name": "Exercise 8"
            }
        ],
        "enabled_on": {  
            "templates": ["index"]
       }
    }
{% endschema %}