{% render 'exercise-5' %}

{% capture solution %}
    {% render 'delimeters' %}
{% endcapture %}

{% render 'section-wrapper', solution: solution %}

{% schema %}
    {
        "name": "Exercise 5",
        "limit": 1,
        
        "settings": [
            {
                "type": "text",
                "id": "values",
                "label": "Numbers",
                "default": "16,4,20,44,25",
                "info": "Enter numbers separated by commas"
            }
        ],
        "presets": [
            {
                "name": "Exercise 5"
            }
        ],
        "enabled_on": {  
        "templates": ["index"]
       }
    }
{% endschema %}